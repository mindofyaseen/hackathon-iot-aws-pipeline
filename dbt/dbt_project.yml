name: hackathon_iot
version: "1.0.0"
config-version: 2

profile: hackathon_iot

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  hackathon_iot:
    silver:
      +schema: CLEAN
      +materialized: view
    gold:
      +schema: ANALYTICS
      +materialized: table
